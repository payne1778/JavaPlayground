# RunCodeOnError

Create something similar to Angular's `catchError` in Java. Including a `CatchErrorRunnable` param in your method will allow you to run arbitrary Java code in context to a specific error that was raised inside your method.

```bash
java RunCodeOnError/RunCodeOnError.java
```
